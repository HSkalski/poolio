import React from 'react';
import PropTypes from 'prop-types';

// When on home page, render ControlPanel
const ControlPanel = () => (
  <div className="Panel ControlPanel">
    I'll be the control panel
  </div>
);

ControlPanel.propTypes = {
  
};

export default ControlPanel;